<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.unice.polytech.polynews">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:largeHeap="true">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".renaud.AssocMain"></activity>
        <activity android:name=".renaud.BdaActivity"></activity>
        <activity android:name=".renaud.BdmActivity"></activity>
        <activity android:name=".renaud.BdcActivity"></activity>
        <activity android:name=".renaud.TheatreActivity"></activity>
        <activity android:name=".renaud.PhotoActivity"></activity>
        <activity android:name=".renaud.BdjActivity"></activity>
        <activity android:name=".renaud.PompomActivity"></activity>
        <activity android:name=".renaud.MontagneActivity"></activity>
        <activity android:name=".renaud.BdsActivity"></activity>
    </application>

</manifest>
